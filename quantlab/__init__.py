"""QuantLab: a local laboratory for building and, more importantly, distrusting ML trading strategies."""

from __future__ import annotations

__version__ = "0.1.0"

# One seed to rule them all. Everything that can be seeded reads this by default.
SEED: int = 42
